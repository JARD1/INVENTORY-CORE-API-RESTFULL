info:
  name: registrarse
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8081/auth/register
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
