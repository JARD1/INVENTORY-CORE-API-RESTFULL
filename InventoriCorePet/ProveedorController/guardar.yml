info:
  name: guardar
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8081/api/v1/proveedores
  body:
    type: json
    data: |-
      {
        "nombre": "Distribuidora Tech C.A.",
        "contacto": "Juan Pérez",
        "telefono": "+584141234567",
        "email": "ventas@distribuidoratech.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
