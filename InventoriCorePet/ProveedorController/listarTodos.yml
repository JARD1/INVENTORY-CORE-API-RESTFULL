info:
  name: listarTodos
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8081/api/v1/proveedores
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
