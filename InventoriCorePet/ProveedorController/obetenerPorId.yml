info:
  name: obetenerPorId
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8081/api/v1/proveedores/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
